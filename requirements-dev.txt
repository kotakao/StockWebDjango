# 開發／測試依賴（含執行時依賴）
# 安裝：.venv/Scripts/pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8            # 測試框架
pytest-django>=4.8   # Django 整合（測試 DB、settings）
fakeredis>=2.20      # 測試用 Redis（無需 redis server 即可 ping）
ruff>=0.6            # lint + format
